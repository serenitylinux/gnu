# vim: ft=sh
name="parted"
iteration="1"
version="3.1"
desc="A program for modifying partitions"
bdeps="e2fsprogs"
deps="$bdeps"
arch="any"
flags=""


function configure() {
  ./configure --prefix=/usr \
              --disable-debug \
              --disable-device-mapper \
              --disable-rpath
}

function build() {
	default
}

function installpkg() {
	default
}
