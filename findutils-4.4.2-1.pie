# vim: ft=sh
name="findutils"
iteration="1"
version="4.4.2"
desc="GNU utilities to locate files"
bdeps=('glibc' 'bash')
deps=('glibc' 'bash')
arch=('any')
flags=()

function configure() {
	default
}

function build() {
	default
}

function installpkg() {
	default
}
