# vim: ft=sh
name="gnu-build-tools"
iteration="2"
version="1.0.0"
desc="Packages required to build most gnu packages"
bdeps=('gnu-build-tools')
deps=('libc(+dev)' 'gcc(+dev)' 'bash' 'gawk' 'make' 'tar' 'binutils' 'coreutils' 'sed' 'grep' 'gzip')
arch=('any')
skip_defaults="true"

function configure() {
	none
}

function build() {
	none
}

function installpkg() {
	none
}
