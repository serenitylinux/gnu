# vim: ft=sh
name="gawk"
iteration="1"
version="4.1.0"
desc="GNU version of awk"
bdeps="bash glibc mpfr"
deps="bash glibc mpfr"
arch="any"
flags=""


function configure() {
	./configure --prefix=/usr --libexecdir=/usr/lib --without-libsigsegv
}

function build() {
	default
}

function installpkg() {
	default
}
