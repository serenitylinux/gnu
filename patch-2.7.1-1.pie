# vim: ft=sh
name="patch"
iteration="1"
version="2.7.1"
desc="A utility to apply patch files to original sources"
bdeps="glibc"
deps="glibc"
arch="any"
flags=""


function configure() {
	default
}

function build() {
	default
}

function installpkg() {
	default
}
