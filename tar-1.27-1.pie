# vim: ft=sh
name="tar"
iteration="1"
version="1.27"
desc="Utility used to store, backup, and transport files"
bdeps=('glibc' 'bash')
deps=('glibc' 'bash')
arch=('any')
flags=()


function configure() {
	./configure --prefix=/usr --libexecdir=/usr/lib/tar
}

function build() {
	default
}

function installpkg() {
	default
}
