# vim: ft=sh
name="readline"
iteration="1"
version="6.2"
desc="GNU readline library"
bdeps=('glibc' 'ncurses')
deps=('glibc' 'ncurses')
arch=('any')
flags=()


function configure() {
	sed -i 's|-Wl,-rpath,$(libdir) ||g' support/shobj-conf
	./configure --prefix=/usr
}

function build() {
	default
}

function installpkg() {
	default
}
